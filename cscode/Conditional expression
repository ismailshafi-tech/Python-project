#Conditional expression

#X if condition else Y ------

num = 8

#print("positive" if num > 0  else "negative")

#result = "even" if num % 2 == 0 else "00"
#print(result)

#a = 6
#b = 7
#max_num = a if a>b else b
#min_num = a if a>b else b
#print(max_num)

#age = 11
#status = "adult" if age >= 18 else "Child"
#print(status)

#temperature = 20
#weather = "hot" if temperature > 40 else "cool"
#print(weather)

user_role = "guest"
role = "full access" if user_role == "admin" else "limited access"
print(role)